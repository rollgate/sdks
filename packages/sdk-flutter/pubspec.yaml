name: rollgate
description: Rollgate Feature Flag SDK for Dart/Flutter. Evaluate feature flags with targeting, rollout, and caching.
version: 1.2.0
homepage: https://rollgate.io
repository: https://github.com/rollgate/sdks/tree/main/packages/sdk-flutter
topics:
  - feature-flags
  - feature-toggles

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.0
  crypto: ^3.0.0
