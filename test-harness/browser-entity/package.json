{
  "name": "@rollgate/browser-contract-test-entity",
  "version": "0.0.0",
  "private": true,
  "description": "Contract test service implementation for browser SDK",
  "type": "module",
  "scripts": {
    "start": "tsc --noEmit && vite --open",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@rollgate/sdk-browser": "file:../../packages/sdk-browser"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
